pipeline {
    agent { dockerfile true }
stages {
  stage('Build') {
    steps {
      // One or more steps need to be included within the steps block.
    }
  }

  stage('LINT') {
    steps {
      // One or more steps need to be included within the steps block.
    }
  }

  stage('Test') {
    steps {
      // One or more steps need to be included within the steps block.
    }
  }

}

}
